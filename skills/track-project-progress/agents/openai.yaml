interface:
  display_name: "Project Progress"
  short_description: "ETA-first project progress in one clear card"
  default_prompt: "Use $track-project-progress to show this project's goal, ETA, completion bar, and current task."

policy:
  allow_implicit_invocation: true
